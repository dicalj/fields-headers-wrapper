// required modules
import collect from 'collect.js'

/**
 * 
 */
export default class Headers {

  /**
   * 
   */
  collect() {
    return collect(this.fields())
  }

  /**
   * 
   */
  fields() {
    return [

    ]
  }
}