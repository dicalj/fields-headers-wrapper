// required modules
import collect from 'collect.js'

/**
 * 
 */
export default class Fields {

  /**
   * 
   */
  collect() {
    return collect(this.fields())
  }

  /**
   * 
   */
  fields() {
    return [

    ]
  }
}